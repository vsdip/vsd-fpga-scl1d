# SCL primitive wrappers

This directory is reserved for the VSD-owned Verilog wrappers used by OpenFPGA. The PDK's `c1d.v` remains an external source. Add only wrappers and test models here, with `vsd_` prefixes, and validate their port order against the SCL Liberty and CDL views.
